import { FC } from 'react';
import Layout from '../Layout/Layout';

const ShotsList: FC = () => {
    return (
        <Layout>
            <div className='tasksblock-list'></div>
        </Layout>
    );
};

export default ShotsList;
